para el shell  y utils  sudo env "PATH=$PATH" npm start
siderbar  sudo env "PATH=$PATH" ng serve  --port 1004 --host 0.0.0.0
auth   sudo env "PATH=$PATH" ng serve  --port 1000 --host 0.0.0.0
administra   sudo env "PATH=$PATH" ng serve  --port 1006 --host 0.0.0.0